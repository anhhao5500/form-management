# form-management

# Kiểm tra các file đã thay đổi
git status

# Đưa tất cả thay đổi vào vùng chờ (Staging Area)
git add .

# Lưu lại thay đổi với thông điệp cụ thể
git commit -m "Mô tả nội dung thay đổi tại đây"

# Đẩy code lên nhánh main trên GitHub
git push origin main
